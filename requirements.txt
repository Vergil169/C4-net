fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pytest==8.3.4
pywebview==5.4
pyinstaller==6.11.1

# Optional competition integrations. The demo runs without them, but the
# orchestration layer is shaped so Intent/Planner agents can be swapped to
# LangChain/LangGraph implementations.
langchain>=0.3.0
langgraph>=0.2.0
